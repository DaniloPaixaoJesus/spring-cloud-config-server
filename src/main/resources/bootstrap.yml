server:  port: 8888
spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DaniloPaixaoJesus/spring-cloud-config-repo.git  profiles:    active:    - local